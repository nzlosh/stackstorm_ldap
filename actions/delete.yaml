---
  name: "delete"
  runner_type: "run-python"
  description: "Delete a DN from the directory tree"
  enabled: true
  entry_point: "delete.py"
  parameters:
    ldap_profile:
      type: string
      description: "Used to select the profile configured in the LDAP pack's config.yaml file."
      required: true
    delete_dn:
      type: string
      description: "The DN to be deleted."
      required: true
